# Forged Cacao

A portfolio storefront concept for hyperrealistic chocolate tools.

## Local preview

```bash
npm install
npm run dev
```

The GitHub Pages workflow deploys the production build automatically after every push to `main`.
